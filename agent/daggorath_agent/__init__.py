"""Training harness for the Daggorath Gym environment.

This package holds the agent side of the project: the feature extractor, the
observation wrappers, and the training pipeline that consumes daggorath_gym as
a library. It is a harness you run, not a library others install — import its
modules directly by running from the workspace root.
"""